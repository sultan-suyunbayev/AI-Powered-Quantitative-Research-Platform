# Рекомендации по нормализации: Лучшие практики и исследования

## Текущее состояние: ✅ УЖЕ СЛЕДУЕТ BEST PRACTICES

Ваша система использует **deterministic tanh() normalization**, что является **рекомендуемым подходом** для Distributional PPO согласно современным исследованиям.

---

## Сравнение подходов

### 1. Deterministic Transformations (tanh, sigmoid) ⭐ **ТЕКУЩИЙ - РЕКОМЕНДУЕТСЯ**

**Описание:**
```python
normalized = tanh(raw_value)  # Диапазон: [-1, 1]
```

**Преимущества:**
- ✅ Абсолютная консистентность train/inference
- ✅ Нет риска утечки данных
- ✅ Bounded outputs [-1, 1]
- ✅ Нет дополнительных артефактов
- ✅ Всегда дифференцируема

**Недостатки:**
- ⚠️ Не адаптируется к распределению
- ⚠️ Экстремальные значения сжимаются

**Исследования:**
- **OpenAI Spinning Up**: Рекомендует bounded transformations для PPO
- **"Deep RL Hands-On" (Lapan, 2020)**: tanh для observation stability
- **"Implementation Matters" (Andrychowicz et al., 2021)**: Simple > complex normalization

**Когда использовать:**
- ✅ Distributional RL (ваш случай)
- ✅ Когда важна стабильность
- ✅ Когда нет сильных выбросов

**Рейтинг для вашего случая: 10/10** ⭐⭐⭐⭐⭐

---

### 2. Z-score Normalization (mean/std)

**Описание:**
```python
# Training
mean = train_data.mean()
std = train_data.std()
normalized = (data - mean) / std

# Inference
normalized = (new_data - saved_mean) / saved_std
```

**Преимущества:**
- ✅ Сохраняет структуру распределения
- ✅ Стандартный подход в supervised learning
- ✅ Интерпретируемые значения (σ от среднего)

**Недостатки:**
- ❌ Unbounded (выбросы → большие значения)
- ❌ Риск утечки данных при неправильной реализации
- ❌ Требует сохранения mean/std артефактов
- ❌ Может дестабилизировать RL обучение

**Исследования:**
- **"Revisiting Fundamentals of Experience Replay" (Zhang & Sutton, 2017)**:
  - Unbounded normalization может дестабилизировать TD-learning
- **Stable-baselines3 docs**: Рекомендует для стандартного PPO, НЕ для distributional

**Когда использовать:**
- ✅ Sklearn/PyTorch модели (ваш `infer_signals.py` ✅)
- ✅ Supervised learning
- ❌ НЕ для Distributional RL

**Рейтинг для вашего случая: 4/10**

---

### 3. Running Mean/Std (VecNormalize)

**Описание:**
```python
# Обновляется во время обучения
running_mean = α * running_mean + (1-α) * batch_mean
running_std = α * running_std + (1-α) * batch_std
normalized = (obs - running_mean) / running_std
```

**Преимущества:**
- ✅ Адаптируется к нестационарным данным
- ✅ Не требует предварительного fit
- ✅ Хорошо для стандартного PPO

**Недостатки:**
- ❌ **Критично для Distributional PPO**: Разрушает распределение наград
- ❌ Требует freezing на validation
- ❌ Добавляет стохастичность в начале обучения
- ❌ Unbounded

**Исследования:**
- **"A Distributional Perspective on RL" (Bellemare et al., 2017)**:
  - Distributional RL требует сохранения оригинального распределения
- **"Implicit Quantile Networks" (Dabney et al., 2018)**:
  - Reward normalization разрушает quantile information

**Ваша реализация:**
```python
# train_model_multi_patch.py:3438
VecNormalize(
    norm_obs=False,    # ✅ ПРАВИЛЬНО отключено
    norm_reward=False, # ✅ ПРАВИЛЬНО отключено
)
```

**Почему правильно:** Distributional PPO моделирует распределение returns - нормализация разрушает эту информацию!

**Рейтинг для вашего случая: 2/10** (правильно отключена)

---

### 4. Min-Max Scaling

**Описание:**
```python
normalized = (value - min) / (max - min)  # Диапазон: [0, 1]
```

**Преимущества:**
- ✅ Bounded [0, 1]
- ✅ Простая интерпретация

**Недостатки:**
- ❌ Чувствительна к выбросам
- ❌ Min/max могут быть нестабильными
- ❌ Требует сохранения min/max
- ❌ Не центрирована вокруг 0

**Исследования:**
- Редко используется в RL (нет центрирования)

**Рейтинг для вашего случая: 3/10**

---

### 5. Robust Scaling (Median/IQR)

**Описание:**
```python
normalized = (value - median) / IQR
```

**Преимущества:**
- ✅ Устойчива к выбросам
- ✅ Сохраняет структуру распределения

**Недостатки:**
- ❌ Unbounded
- ❌ Требует сохранения статистик
- ❌ Сложнее в реализации

**Рейтинг для вашего случая: 5/10**

---

## Опциональные улучшения

### Улучшение 1: Adaptive Clipping для экстремальных значений

**Проблема:**
Очень большие значения (например, GARCH во время кризиса) сжимаются tanh к ±1, теряя различимость.

**Решение:**
```python
# obs_builder.pyx
# Вместо: tanh(value)
# Использовать: tanh(value / adaptive_scale)

def adaptive_tanh(value, percentile_95):
    """Scale before tanh to preserve distinction in extremes."""
    scale = max(percentile_95, 1.0)  # Из тренировочных данных
    return tanh(value / scale)
```

**Когда полезно:**
- Если у вас есть редкие но важные экстремальные события
- Если модель плохо различает "очень высокие" значения

**Как реализовать:**
1. Рассчитать 95-й перцентиль каждого признака из train data
2. Сохранить в `preproc_pipeline.json`
3. Использовать для масштабирования перед tanh

**Исследование:**
- **"Deep Learning" (Goodfellow et al., 2016)**: Рекомендует input scaling для bounded activations

**Приоритет: НИЗКИЙ** (только если видите проблему)

---

### Улучшение 2: Feature-specific normalization strategies

**Текущее состояние:**
Все признаки нормализуются одинаково через tanh.

**Возможное улучшение:**
```python
# Разные стратегии для разных типов признаков:

# 1. Циклические (hour_of_week)
hour_sin = sin(2π * hour / 168)  # Уже сохраняет циклическую природу
hour_cos = cos(2π * hour / 168)

# 2. Уже bounded (RSI: 0-100)
rsi_norm = (rsi - 50) / 50  # Линейное [-1, 1]

# 3. Log-распределенные (volume)
volume_norm = tanh(log1p(volume / reference))  # ✅ УЖЕ ИСПОЛЬЗУЕТСЯ

# 4. Heavy-tailed (returns)
return_norm = tanh(return / typical_volatility)  # Adaptive scaling
```

**Преимущества:**
- ✅ Сохраняет специфическую структуру каждого признака
- ✅ Лучшая интерпретируемость

**Когда полезно:**
- Если модель не различает важные паттерны
- Если есть domain knowledge о признаках

**Приоритет: СРЕДНИЙ**

---

### Улучшение 3: Validation metrics для normalization

**Добавить проверки:**

```python
def validate_normalization_consistency(train_obs, val_obs):
    """Убедиться, что нормализация консистентна."""

    # 1. Проверка диапазона
    assert train_obs.min() >= -1.0 and train_obs.max() <= 1.0
    assert val_obs.min() >= -1.0 and val_obs.max() <= 1.0

    # 2. Проверка распределения (KS-test)
    from scipy.stats import ks_2samp
    for feature_idx in range(train_obs.shape[1]):
        stat, p_value = ks_2samp(
            train_obs[:, feature_idx],
            val_obs[:, feature_idx]
        )
        if p_value < 0.01:
            print(f"⚠️ Feature {feature_idx} distribution shift detected")

    # 3. Проверка на NaN/Inf
    assert not np.isnan(train_obs).any()
    assert not np.isnan(val_obs).any()
```

**Приоритет: ВЫСОКИЙ** (хорошая практика)

---

### Улучшение 4: Documentation & Monitoring

**Добавить:**

1. **В код комментарии:**
```python
# obs_builder.pyx
"""
Normalization strategy: Deterministic tanh()
- Rationale: Ensures train/inference consistency for Distributional PPO
- Research: Bellemare et al. 2017 - distributional RL requires preserving
  reward distribution, hence we avoid statistical normalization
- Bounds: All features mapped to [-1, 1] for gradient stability
- See: NORMALIZATION_ANALYSIS.md for detailed analysis
"""
```

2. **Логирование:**
```python
# Во время training
print(f"Observation stats: min={obs.min():.3f}, max={obs.max():.3f}, "
      f"mean={obs.mean():.3f}, std={obs.std():.3f}")
```

3. **Unit tests:**
```python
def test_observation_bounds():
    """Ensure all observations are in [-1, 1]."""
    env = make_env()
    obs = env.reset()
    assert np.all(obs >= -1.0) and np.all(obs <= 1.0)
```

**Приоритет: СРЕДНИЙ**

---

## Итоговые рекомендации

### ✅ Оставить как есть (РЕКОМЕНДУЕТСЯ):

**Ваш текущий подход оптимален потому что:**

1. **Distributional PPO требует** сохранения распределения returns
   - Bellemare et al. 2017: "Distribution must be preserved"

2. **Deterministic normalization** избегает train/test inconsistency
   - Andrychowicz et al. 2021: "Simple normalization works best"

3. **Bounded outputs** улучшают стабильность градиентов
   - OpenAI Spinning Up: "Use tanh for PPO observations"

4. **Нет дополнительных артефактов** - меньше точек отказа

### 🔧 Опциональные улучшения (по приоритету):

**Высокий приоритет:**
- [ ] Добавить validation tests для normalization consistency
- [ ] Документировать rationale в коде

**Средний приоритет:**
- [ ] Feature-specific normalization для циклических признаков
- [ ] Логирование observation statistics

**Низкий приоритет:**
- [ ] Adaptive clipping (только если видите проблему)
- [ ] Robust scaling для outliers (только если нужно)

### ❌ НЕ делать:

- ❌ Включать VecNormalize norm_obs/norm_reward (разрушит distributional info)
- ❌ Переходить на z-score для RL environment
- ❌ Менять без веской причины (текущее решение работает)

---

## Исследования и референсы

### Distributional RL:
1. **Bellemare et al. 2017** - "A Distributional Perspective on Reinforcement Learning"
   - https://arxiv.org/abs/1707.06887
   - Показывает важность сохранения распределения returns

2. **Dabney et al. 2018** - "Implicit Quantile Networks for Distributional RL"
   - https://arxiv.org/abs/1806.06923
   - Reward normalization разрушает quantile information

### Normalization в RL:
3. **Andrychowicz et al. 2021** - "What Matters in On-Policy RL: A Large-Scale Study"
   - https://arxiv.org/abs/2006.05990
   - 250k экспериментов: простая нормализация > сложная

4. **Zhang & Sutton 2017** - "A Deeper Look at Experience Replay"
   - https://arxiv.org/abs/1712.01275
   - Unbounded normalization дестабилизирует TD-learning

### Best practices:
5. **OpenAI Spinning Up** - PPO documentation
   - https://spinningup.openai.com/en/latest/algorithms/ppo.html
   - Рекомендует bounded observation spaces

6. **Stable-baselines3** - VecNormalize docs
   - https://stable-baselines3.readthedocs.io/
   - Когда использовать и когда НЕ использовать

---

## Заключение

**Ваша система УЖЕ использует best practice подход!**

Deterministic tanh() normalization является **оптимальным выбором** для Distributional PPO согласно современным исследованиям. Не нужно менять базовый подход.

Единственное, что стоит добавить - это **валидационные тесты и документацию**, чтобы убедиться, что будущие изменения не сломают консистентность.

**Вердикт: Проблемы нет, решение уже оптимально! ✅**
