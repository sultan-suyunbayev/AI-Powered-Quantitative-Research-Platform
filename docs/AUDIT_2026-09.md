# Repository audit — September 2026

Scope: full tracked tree at `main@d1f5881` (1 785 commits, 3 454 tracked files, ~50 MB on GitHub).
Purpose: prepare the repository for a public Apache-2.0 release (v1.0.0).

Severity: **P0** blocks the release · **P1** should be fixed before the release · **P2** cosmetic / nice to have.

---

## 1. Secrets and private data

| # | Finding | Sev | Status / proposal |
|---|---------|-----|-------------------|
| 1.1 | No live credentials found. Scanned for `sk-*`, `ghp_*`, `github_pat_*`, `AKIA*`, `AIza*`, `xox*-*`, PEM private keys, JWTs, and `key/secret/token/password = "<literal>"` assignments across all tracked files. Every hit is a test fixture (`AKIAIOSFODNN7EXAMPLE` is AWS's own documented example key) or a `.secrets.baseline` hash. | — | No action. |
| 1.2 | No real e-mail addresses of third parties. All 300+ addresses are synthetic (`@example.com`, `@ccea.io`, `@rivenquant.com`) or public regulator contacts (BaFin, AMF, FCA, ESMA) inside compliance documents. | — | No action. |
| 1.3 | `.env.example` contains only empty keys — correct. | — | No action. |
| 1.4 | **Local Windows paths leak the owner's user name.** `PRO_MODE_DESIGN_DOCUMENT.md` has ~120 `file:///c:/Users/<name>/StartUp/...` links; `models/dataset_registry.json`, `models/tcost_calibration.json`, `experiments/*/meta.json`, `model_registry/**/*.sig`, `docs/CCEA_DESKTOP.md`, `docs/agent/INSTALLATION.md`, `index.html` and 175 `scratch_*` files also embed them. 181 tracked files total. | P1 | Rewrite `file:///…` links to repo-relative paths; delete the scratch/registry artifacts (see §3). |
| 1.5 | Private RFC1918 IPs (`10.*`, `192.168.*`) appear only in compliance/network-design docs as examples. | P2 | Leave. |

**Conclusion: nothing needs to be purged from history for secrecy reasons.** History rewriting (§7) is motivated by attribution noise and size, not by leaked credentials.

## 2. Data files — licensing risk

| # | Finding | Sev | Status / proposal |
|---|---------|-----|-------------------|
| 2.1 | **`data/raw_stocks/*.parquet` is real market data downloaded from Yahoo Finance** — SPY, QQQ, IWM, GLD, VIX, AAPL, 2021–2024 (verified: real prices/volumes; commit `1d4e09a` says so explicitly). Yahoo Finance's terms prohibit redistribution. | **P0** | **Needs your decision.** Proposal: remove from the repo, keep `scripts/download_stock_data.py` as the fetch path. |
| 2.2 | `data/prices.parquet`, `data/features.parquet`, `data/train.parquet`, `data/test_*.parquet`, `data/stocks/SPY_*.parquet` (~15 MB) are **derived** from the same Yahoo data (identical 3 485-row shape). | **P0** | Same decision as 2.1. Derived features are a weaker claim than raw bars, but same source. |
| 2.3 | `data/raw_forex/*`, `data/forex/*` are **synthetic** (commit `d036ef6`: "synthetic forex data for testing"). | — | Keep. |
| 2.4 | `data/fundamentals_edgar/edgar_pit.parquet` — SEC EDGAR, public domain. | — | Keep. |
| 2.5 | `data/raw_options/AAPL_options.parquet` — options chain, provenance not recorded in the commit. | P1 | **Needs your decision** — if it came from a paid provider it must go. |
| 2.6 | `prepare_demo_data.py` already generates synthetic OHLCV, but nothing in the docs points at it. | P1 | Wire it into Quick Start so the repo works with no bundled data. |

## 3. Junk and working artifacts

| # | Finding | Sev | Size | Proposal |
|---|---------|-----|------|----------|
| 3.1 | **175 `scratch_*` files in the repo root** (`scratch_raw_step_*.json`, `scratch_content_*.txt`, `scratch_view_step_*.txt`, `scratch_search.py`) — raw agent step dumps. | **P0** | 0.8 MB | Delete. |
| 3.2 | `archive/` — 481 files of historical AI reports. **But** `archive/root_files/Design Doc CCEA Cloud.txt` is referenced by `README.md`, `DOCS_INDEX.md`, `BUSINESS_OVERVIEW.md`, `docs/CCEA_OVERVIEW.md`. | P1 | 3.8 MB | Move `archive/root_files/` → `docs/history/`, delete the rest. |
| 3.3 | `essential_docs_collection/` — 31 files, a pure duplicate: 20 byte-identical to their `docs/` counterparts, 5 stale forks. | P1 | 0.7 MB | Delete. |
| 3.4 | Root artifacts: `coverage.json`, `git_diff_index.txt`, `git_diff_index_clean.patch`, `git_diff_index_utf8.patch`, `index_diff.patch`, `index_reconstructed.html`, `read_logs_output.txt`, `read_logs.py`. | P1 | 0.7 MB | Delete. |
| 3.5 | Root status reports: `P0_/P1_/P2_BLOCKERS_CLOSURE.md`, `PLATFORM_FULL_GAP_ANALYSIS_2026-07-15.md`, `PRO_PIPELINE_GAP_ANALYSIS.md`, `REPO_SPLIT_MAPPING.md`, `RELEASE_CHECKLIST.md`, `MVP_DOCUMENTATION.md`, `QUICK_START_REFERENCE.md`, and the `PROJECT_/PRODUCT_/BUSINESS_OVERVIEW.md` trio. 25 root `.md` in total. | P1 | — | Keep 9 (README, ARCHITECTURE, CONTRIBUTING, CHANGELOG, LICENSE, NOTICE, SECURITY, BUILD_INSTRUCTIONS, QUICK_START); move the rest to `docs/` or delete. |
| 3.6 | Reports inside `tests/`: `COMPREHENSIVE_TEST_REPORT.md`, `TEST_EXECUTION_SUMMARY.md`, `README_AWR_TESTS.md`, `README_QUANTILE_LEVELS.md`, `README_ratio_clamping.md`. | P1 | — | Delete (`tests/` should hold tests only). |
| 3.7 | `reports/` (6), `audits/` (1), `issues/` (2) — leftover run output. `reports/XS_*_TRUST_REPORT.md` document real backtest runs. | P2 | — | Move the two trust reports to `docs/history/`, delete the rest. |
| 3.8 | `web_assets/monaco/` — 112 files of a vendored Monaco editor. It **is** used: `index.html` loads `/assets/monaco/vs/loader.js`. | P1 | 12.9 MB | Replace with the jsDelivr CDN build, or keep and record it in `NOTICE` (Monaco is MIT). |
| 3.9 | `index.html` (2.5 MB) is **not junk** — it is the live Streamlit/desktop UI served by `app.py` and `desktop_backend.py`. | — | 2.5 MB | Keep. |
| 3.10 | `desktop/` is a real Tauri shell (22 files, Rust + build scripts), not a stub. | — | 0.2 MB | Keep. |
| 3.11 | 14 root Python modules are referenced by nothing: `di_stubs.py`, `domainorder_manager.py`, `fast_lob__init__.py`, `features__init__.py`, `featuresbasic.py`, `featuresregistry.py`, `legacy_bridge.py`, `normalization_utils.py`, `portfolio_allocator.py`, `read_logs.py`, `scratch_search.py`, `script_compare_runs.py`. (`sitecustomize.py` is auto-imported by Python and `prepare_demo_data.py` is a CLI — both stay.) All 17 `.pyx` files are live build targets in `setup.py`. | P1 | — | Delete the 12 dead ones; leave every other root module in place (imports are by bare module name). |

## 4. Build and tests

| # | Finding | Sev | Status |
|---|---------|-----|--------|
| 4.1 | **`python setup.py build_ext --inplace` failed on Windows/MSVC.** `info_builder`, `environment` and `risk_manager` declare `# distutils: language = c++` in their `.pyx` but `setup.py` compiled them with `language="c"` and `c_args`, which omit `/std:c++17`. MSVC then defaults to C++14, `std::optional` in `AgentOrderTracker.h` does not exist, and the build dies with `C2065: cand_le`. GCC defaults to `gnu++17`, so Linux was unaffected — the bug was Windows-only. | **P0** | **Fixed** — those three extensions now use `language="c++"` + `cxx_args`. All 17 extensions build. |
| 4.2 | `tests/test_ccea_documentation_guardrails.py` errors at collection time (`Failed: 'documentation' n...`), aborting the whole run under `-x`. | P1 | To investigate. |
| 4.3 | 21 876 tests collect. Full-run pass/fail numbers are recorded separately (run in progress at the time of writing). | — | — |
| 4.4 | Several tests skip when the Cython modules are absent, with clear reasons — correct behaviour. | — | No action. |

## 5. Dependencies — three independent breakages

| # | Finding | Sev | Status |
|---|---------|-----|--------|
| 5.1 | **`requirements-cpu.lock.txt` cannot be installed at all.** `shimmy==2.0.0` requires `gymnasium>=1.0`, the file pins `gymnasium==0.29.1`. `requirements-gpu.lock.txt` has the identical conflict. This is the single root cause of the red CI (§6.1). | **P0** | Fix: `shimmy==1.3.0`. |
| 5.2 | `requirements-cpu.lock.txt` pins `cryptography==42.0.8`, `requirements-dev.lock.txt` pins `43.0.3` — they cannot be resolved in one pass. | P1 | Align on one version. |
| 5.3 | `requirements-dev.lock.txt` pins `fastapi==0.115.6` but not `pydantic`, so pip upgrades `pydantic` to 2.13.x while `pydantic_core` stays pinned at `2.23.4` → every import raises `SystemError: incompatible pydantic-core version`. | **P0** | Pin `pydantic`/`pydantic_core` consistently in both lock files. |
| 5.4 | `statsmodels` is pinned in no lock file. pip installs 0.15.0, whose changed `deprecate_kwarg` signature breaks `arch==7.1.0` at import → `transformers.py` and everything downstream fail. | **P0** | Pin `statsmodels==0.14.4`. |
| 5.5 | Together, 5.1–5.4 mean the lock files are not reproducible in the sense they claim. A clean `pip install -r requirements-cpu.lock.txt` in a fresh venv does not work today. | **P0** | Regenerate both lock files and add a CI job that installs them from scratch. |

## 6. CI — every workflow is red

| # | Finding | Sev | Status |
|---|---------|-----|--------|
| 6.1 | `build-and-test.yml` has failed on every run since at least 2026-07. Cause: the `pip install -r requirements-cpu.lock.txt` step exits 1 (§5.1), after which every test errors with `ModuleNotFoundError: No module named 'pydantic' / 'yaml' / 'cryptography'`. **The workflow exists and does run pytest** — contrary to the assumption that only docs-quality and SAST were wired up. | **P0** | Fixed by §5. |
| 6.2 | `docs-quality.yml` fails on markdownlint: hundreds of MD013/MD022/MD031/MD032/MD040 violations across `ARCHITECTURE.md`, `adversarial/README.md` and others. | P1 | Auto-fix what is auto-fixable, relax `.markdownlint.json` for the rest. |
| 6.3 | `security-sast.yml` fails for two independent reasons: `pip-audit` is invoked with `--disable-pip` on non-hashed requirements files (rejected), and `safety` exits 64 because it now requires an account. | P1 | Drop `--disable-pip`; replace `safety` with `pip-audit` alone. |
| 6.4 | `actions/checkout@v4` and `actions/upload-artifact@v4` emit Node 20 deprecation warnings. | P2 | Bump to `@v5`. |

## 7. Git history and public GitHub surface

Facts as measured:

| Metric | Value |
|--------|-------|
| Commits | 1 785 |
| Authors | RivenKid69 1 313 · `Claude <noreply@anthropic.com>` 254 · `codex-cli` 191 · `Antigravity Agent` 19 · `Suyunbayev Kairat` 7 · `RivenKid69 <…users.noreply>` 1 |
| `Co-Authored-By` trailers | 575 |
| Commit messages mentioning Claude/Codex/Copilot/"Generated with" | 1 734 of 1 785 |
| Tracked files mentioning AI tools (outside `archive/`, `reports/`, `audits/`, `issues/`) | 158 |
| **Remote branches** | **526 — of which 352 `codex/*` and 169 `claude/*`** |
| **Pull requests** | **496, titled and branched by the agents (`claude/phase-11-implementation`, `codex/revise-code-comments-…`); 10 still open** |
| Contributors tab | `sultan-suyunbayev`, `claude`, `codex-cli`, `Antigravity-Agent`, `Kairat0507` |
| Largest blobs in history | `lob_state_cython…so` 7.0 MB, `tsWorker.js` 4.4 MB, `.run/frozen-pipeline-20260714/*.parquet` 4.2+4.1+4.0 MB, 12 copies of `index.html` at 2.4 MB each |

The 526 agent-named branches and 496 agent-titled PRs were **not** in the original scope and are the most visible AI trace on the repository page — more visible than the commit trailers. Branches can be deleted through the API; **pull requests cannot be deleted**, only closed, and their titles stay in the PR tab forever.

| # | Finding | Sev | Proposal — all of these need your explicit yes |
|---|---------|-----|-----------|
| 7.1 | Agent identities as commit authors/committers. | P1 | `git filter-repo --mailmap` → `Sultan Suyunbayev <sulastomatolog@gmail.com>`. **Requires force-push.** |
| 7.2 | 575 `Co-Authored-By` trailers and 1 734 tool mentions in messages. | P1 | `--message-callback` strips trailers and "Generated with …" lines, keeps the meaning. |
| 7.3 | 7 commits by `Suyunbayev Kairat <kaermamulya97@gmail.com>` (all 2025-12-14). | — | **Question for you:** a different person, or your own commits from another machine? Untouched by default. |
| 7.4 | 521 agent-named remote branches. | P1 | Delete via `gh api -X DELETE`. Independent of history rewriting. |
| 7.5 | 496 PRs with agent titles/branches, 10 open. | P1 | **Question for you.** They cannot be deleted. Options: (a) live with them; (b) recreate the repository from the cleaned history — the repo has 0 stars and 0 forks, so the only loss is the PR/issue history and the creation date. |
| 7.6 | `docs/AI_GUIDE.md`, `docs/codex/`, `essential_docs_collection/AI_GUIDE.md` — AI-assistant instructions. `claude.md` is already deleted but still linked from 15 documents (all dead links). | P1 | Delete; move anything useful into `CONTRIBUTING.md`. |
| 7.7 | Repo weight: 3.8 MB `archive/` + 18 MB `data/` + 12.9 MB `web_assets/` in the tree, plus dead blobs in history (`.run/` parquet files, 12 `index.html` revisions, compiled `.so`). | P2 | `filter-repo --invert-paths` on the same pass, if you approve the rewrite. |

## 8. Licensing and legal layer

| # | Finding | Sev | Proposal |
|---|---------|-----|----------|
| 8.1 | `LICENSE` is a 10-line **"PROPRIETARY AND CONFIDENTIAL / All rights reserved"** notice. GitHub reports `spdx_id: NOASSERTION`. | **P0** | Replace with the verbatim Apache License 2.0. |
| 8.2 | `pyproject.toml`: `license = {text = "Proprietary"}`, classifier `License :: Other/Proprietary License`, `authors = [{name = "TradingBot2 Team"}]`. | **P0** | → `Apache-2.0`, `License :: OSI Approved :: Apache Software License`, author `Sultan Suyunbayev`. |
| 8.3 | `LICENSING.md` describes an open-core split plan (`ccea-sdk` / `ccea-agent` public, `ccea-cloud` proprietary). | P1 | Replace with one paragraph, or delete. |
| 8.4 | `README.md` states "This monorepo is **proprietary**". 158 files mention "proprietary" / "trade secret" / "all rights reserved". | **P0** | Remove from README and the live docs. |
| 8.5 | `docs/business/` — 17 documents: `TRADE_SECRET_POLICY.md`, `IP_PROTECTION_STRATEGY.md`, `PATENT_CLAIMS_DRAFT.md`, `PRICING_DIFFERENTIATION_STRATEGY.md`, `COMPETITIVE_MOAT.md`, `OPEN_CORE_BUSINESS_MODEL.md`, `INVESTOR_IP_SUMMARY.md`, `TESTIMONIAL_ACQUISITION_STRATEGY.md`, `PROJECTED_CASE_STUDIES.md`, … | P1 | Move to `docs/history/business/` with a "historical, superseded by Apache-2.0" header, or delete. |
| 8.6 | `tools/repo_split/templates/*/LICENSE` + `LICENSING.md` — three more proprietary licence stubs. | P1 | Update or delete together with the split plan. |
| 8.7 | No `NOTICE`, no `SECURITY.md`, no `CODE_OF_CONDUCT.md`. | **P0** | Add all three. |
| 8.8 | `investor-landing.html`, `investor-landing-ru.html` in the root — investor marketing pages. | P1 | Delete or move to `docs/history/`. |

## 9. Code quality

| # | Finding | Sev | Status |
|---|---------|-----|--------|
| 9.1 | `ruff check` reports ~46 000 violations against the project's **own** `pyproject.toml` config (`E,W,F,I,B,C4,UP`) — the rules were declared but never enforced. Largest buckets: `UP006` 13 716, `UP007` 11 792, **`F401` unused-import 5 881**, `I001` 2 879. | P1 | Apply only auto-fixable and safe rules; do not touch logic. |
| 9.2 | **`F821` undefined-name — 61 hits, these are real latent `NameError`s.** Notably `execution_sim.py:11960,12131` (`plan_queue`, `original_planned_qty`), `impl_greeks.py:802` / `impl_greeks_vectorized.py:849` / `impl_iv_calculation.py:736` / `impl_pricing.py:1004` (`date`), `impl_panel.py:172` (`os`), `scripts/fetch_binance_filters.py:464,487` (`logging`), `core_conformal.py:425,426,472` (`np`), `app.py:9816` (`logger`), `lob/lazy_multi_series.py:144,200` (`Decimal`). | P1 | Report only — not fixed silently. Most are missing imports on error paths. |
| 9.3 | `ruff format --check`: 1 744 of 2 046 files would be reformatted. | P2 | Run `ruff format` once. |
| 9.4 | 305 tracked text files have CRLF endings; no `.gitattributes` exists. No BOM found. | P2 | Add `.gitattributes` with `* text=auto eol=lf`, normalise. |
| 9.5 | 1 088 bare `print()` calls in non-CLI modules; 50 `sys.path` manipulations; 6 bare `except:`; 6 `TODO/FIXME/HACK`. | P2 | Leave; noting for the record. |
| 9.6 | `transformers.py` in the repo root shadows the PyPI `transformers` package name. | P2 | Known trade-off of the flat root layout; renaming would break imports. Leave, document. |

## 10. Documentation

| # | Finding | Sev | Proposal |
|---|---------|-----|----------|
| 10.1 | 743 markdown files, 1 963 relative links, **1 129 broken in 166 files**. The bulk are inside `archive/` and disappear with it; 22 broken links live in real docs, 15 of them pointing at the deleted `claude.md`. | P1 | Fix after the cleanup; add a link-check script. |
| 10.2 | 30 duplicated document basenames across `docs/`, the root and `essential_docs_collection/`. | P1 | Deduplicate (§3.3, §3.5). |
| 10.3 | `DOCS_INDEX.md` describes a structure that no longer exists. | P1 | Regenerate. |
| 10.4 | `README.md` is 234 lines, opens with B2B positioning and "proprietary", and hedges throughout ("is designed not to…") instead of pointing at the import-boundary tests that actually enforce it. | **P0** | Rewrite (≤120 lines). |

## 11. Git hygiene

| # | Finding | Sev | Proposal |
|---|---------|-----|----------|
| 11.1 | `.gitignore` is thorough (`.venv`, `__pycache__`, `*.so`/`*.pyd`, `build/`, `logs/`, `artifacts/`, `.env`, `.claude/`, `.codex/`, `.cursor/`). | — | Add `*.parquet` if the data goes (§2). |
| 11.2 | Three tracked filenames contain spaces or Cyrillic, all under `archive/root_files/`. | P2 | Rename on the move to `docs/history/`. |
| 11.3 | No `.gitattributes`. | P2 | Add (§9.4). |

---

## Top 10, in priority order

1. `LICENSE` is proprietary — the whole point of the release (§8.1, 8.2, 8.4).
2. `requirements-cpu.lock.txt` does not install; all three lock files are broken in four independent ways (§5).
3. CI has been fully red for months, with §5 as the root cause (§6).
4. `data/raw_stocks/` and everything derived from it is Yahoo Finance data under a no-redistribution term (§2.1, 2.2) — **your decision**.
5. Windows/MSVC native build was broken (§4.1) — **fixed**.
6. 526 remote branches and 496 pull requests named by the agents (§7.4, 7.5) — **your decision**.
7. 175 `scratch_*` agent dumps in the repo root (§3.1).
8. 481 files of AI reports in `archive/`, 31 duplicated documents in `essential_docs_collection/` (§3.2, 3.3).
9. 61 `F821` undefined names — real latent `NameError`s (§9.2).
10. README hedges and sells instead of showing the engineering (§10.4).

## Decisions needed from you

1. **Yahoo Finance data (§2.1, 2.2)** — remove ~17 MB of `data/*.parquet` and make Quick Start run on `prepare_demo_data.py` + `scripts/download_stock_data.py`?
2. **`data/raw_options/AAPL_options.parquet` (§2.5)** — where did it come from?
3. **History rewrite (§7.1, 7.2)** — force-push after a mirror backup? All SHAs change; existing clones break; there are 0 forks.
4. **Kairat's 7 commits (§7.3)** — leave as they are, or map to you?
5. **496 pull requests (§7.5)** — live with them, or recreate the repository?
